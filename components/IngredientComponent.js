import React, { Component } from 'react';

export default class IngredientComponent extends Component {
    render() {
        return (
                {/*<Image source={require(this.props.path)} style={{width: 100, height: 100}}/>*/}
        );
    }
}
